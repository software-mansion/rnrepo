{
  "name": "@rnrepo/config",
  "description": "Shared configuration for React Native library builds",
  "type": "module",
  "main": "./src/index.ts",
  "private": true,
  "scripts": {
    "validate": "bun run src/validate.ts"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "ajv": "^8.17.1",
    "ajv-formats": "^3.0.1"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}

