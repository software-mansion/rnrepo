name: Publish Expo Config Plugin to npm

run-name: Publish @rnrepo/expo-config-plugin to npm

on:
  workflow_dispatch:

jobs:
  publish-expo-config-plugin:
    runs-on: ubuntu-latest
    
    defaults:
      run:
        working-directory: ./packages/expo-config-plugin

    steps:
      - name: 🏗 Checkout repository
        uses: actions/checkout@v4

      - name: 🏗 Setup Bun
        uses: oven-sh/setup-bun@v1
        with:
          bun-version: latest

      - name: 📦 Install dependencies
        run: bun install

      - name: 🔨 Build plugin
        run: bun run build

      - name: 🔨 Prepare package
        run: bun run prepare

      - name: 📃 Create package tarball
        run: npm pack

      - name: 🏷️ Set Version env variable
        run: echo "VERSION=$(bun run -c 'node -e \"console.log(require(\\\"./package.json\\\").version)\"')" >> $GITHUB_ENV

      - name: 🏷️ Set PACKAGE_NAME env variable
        run: echo "PACKAGE_NAME=$(ls -l | egrep -o "@rnrepo/expo-config-plugin-(.*)(=?\.tgz)")" >> $GITHUB_ENV

      - name: ❌ Fail if PACKAGE_NAME  and VERSION is not set
        if: ${{ env.PACKAGE_NAME == '' || env.VERSION == '' }}
        run: exit 1 # This should never happen.

      # - name: 🏷️ Create Git tag
      #   run: |
      #     git tag "expo-config-plugin-v${{ env.VERSION }}"
      #     git push origin "expo-config-plugin-v${{ env.VERSION }}"

      # TODO: Temporary use tag nightly
      - name: 🚀 Publish to npm
        run: npm publish $PACKAGE_NAME --tag nightly --provenance